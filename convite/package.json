{
  "name": "lirandzo-adminmanager-backend",
  "version": "1.1.0",
  "description": "Backend central Render + MongoDB para gestão multi-cliente de convites Lirandzo.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check adminmanager-operator.js && node --check adminmanager-script.js && node --check adminmanager-inline-check.js && node --check adminmanager_script_check.js",
    "test:bot": "node tests/operator-v8.test.js",
    "verify": "npm run check && npm run test:bot"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "mongoose": "^8.19.2",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
